# Product Decisions

Decision log for the Culinary Tools Platform. Records approved product/business decisions and rejected alternatives so they don't have to be re-litigated or re-discovered from conversation memory. Maintained by Scribe.

This is a log of **approved** decisions only. Proposals, brainstorming, and Architect design options that were not approved by a Registered Admin do not belong here — see [MISSION_TEMPLATE.md](MISSION_TEMPLATE.md) for how a proposal becomes a decision. Standing system-architecture rules (data authority, storage rules, protected tool integrity rules) live in [ARCHITECTURE_RULES.md](ARCHITECTURE_RULES.md), not here, to avoid duplicating the same rule in two places.

## How to log a new decision

When a mission produces an approved product decision, Scribe adds an entry below with: date, decision, source/approval, and rejected alternatives (if any were explicitly considered and turned down). Do not record a decision that isn't already established in-thread or in existing repo files — if in doubt, ask Chief rather than infer.

## Decisions

### 2026-08-28 - Platform Home tool sections and SSMT modifier usability
Tyler approved reorganizing Platform Home as a tool hub with two visible sections. `Chef Tools` should show, in order: `Neighborhood Rotations`, `Menu Library`, `Menu Engineering`, `Menu Cross Utilization Tool`, and `Webtrition`. `Programming & Auditing` should show, in order: `SSMT`, `Menu Projects`, `Menu Audit Tool`, and `Lean Tool`. This is a grouping/order refinement only; it does not approve a broader Platform Home redesign, workflow-category replacement, tool rename, or navigation behavior change.

Tyler also approved an SSMT usability follow-up for large menu programming records: denser menu-selector organization with stronger menu-type contrast; visible internal scrolling for wide builder tables without relying on bottom-of-page horizontal scroll; wider and more fluid editable item name/description fields; and simple click-confirm deletion for app-side SSMT menu, item, modifier-group, and modifier-item records instead of exact-name retyping. This click-confirm change does not grant deletion authority over Webtrition, Menu Library, source workbook records, submitted operational history, Supabase data, or any non-SSMT operational source.

Modifier groups should be editable in the app-side SSMT record model: users can type the modifier group name, set the type as `Force`, `Remove`, or `Addition`, add/delete modifier item lines, and edit modifier name, description, exact MRN text, calories, and pricing-structure selection. Prices remain tied to the existing Pricing Structure rows and area prices remain displayed in Tyler's required order: `AUS, BNA, BOS, BWI, DEN, IAD, JFK, LAX, SAN, SNA, SEA, SJC, WAS, YVR, YYZ, MCO`. This pass keeps modifier edits in guarded browser-persisted app-side SSMT records and does not make them shared Supabase organizational truth. Requesting Admin: Tyler. Approving Admin: Tyler. Admin of Record: Tyler.
Follow-up on 2026-08-28: Tyler approved making the modifier detail popup wider and coloring the row-level `View modifiers` button so it stands out. This is a UI usability refinement only and does not change modifier persistence, pricing authority, source authority, or deletion authority.
Second follow-up on 2026-08-28: Tyler approved tightening the modifier item-line display so more rows are visible at once, with smaller item-line controls, reduced vertical gaps, and darker item borders. This remains a UI density refinement only and does not change modifier persistence, pricing authority, source authority, area price order, exact MRN handling, or deletion authority.
Third follow-up on 2026-08-28: Tyler approved tightening the selected-menu builder itself so a maximized SSMT screen uses the available desktop width and shows more menu rows at once. This remains a UI density/layout refinement only and does not change SSMT menu/modifier data behavior, pricing authority, source authority, area price order, exact MRN handling, persistence authority, or deletion authority.
Fourth follow-up on 2026-08-30: Tyler approved locking the selected-menu builder at a readable maximum width so the wide SSMT editor does not over-stretch on maximized ultra-wide screens. Tyler also approved making area prices slightly larger, adding clearer alternating row color, making phase/status more prominent under the editor navigation, adding item-row locks for Culinary completion, making locked fields and prices click-to-copy for Centric, and requiring all item rows to be locked before moving to the next phase. Locked rows must be unlocked before editing, repricing, or deletion. This remains an app-side SSMT UI/workflow refinement only and does not change SSMT shared persistence authority, pricing authority, source authority, area price order, exact MRN handling, modifier behavior, or deletion authority.
Fifth follow-up on 2026-08-30: after Tyler asked whether the Lock button saved across the app for other people and Chief answered that it was still local browser workflow only, Tyler approved making SSMT lock/save state shared. The approved shared persistence target is the normal app-side SSMT workspace state: menus, item rows, item lock state, phase/status, pricing rows, modifier groups, and selected-menu state should load/save through the existing secure storage API so Culinary and IT can work from the same shared SSMT state across users/devices. This does not approve a production schema migration, manual production-data rewrite, Webtrition/MenuWorks source-authority change, pricing-source change, area price order change, exact MRN coercion, modifier authority change, deletion authority change, or downstream eligibility change.
Sixth follow-up on 2026-08-31: Tyler approved adding an SSMT selected-menu export for IT/Centric on the item-lines screen and correcting the SSMT menu selector's missing Global menu grouping against the posted reference screenshot. The export should produce `[Brand Name] SSMT Export.xlsx` from app-side SSMT menu/modifier data and follow the uploaded Centric export workbook shape as completely as current SSMT data allows, using placeholders for missing non-ID fields that require IT review. Centric-owned IDs should not be invented. Global menus should be grouped like the screenshot and alphabetized in the SSMT selector. This approval does not change Webtrition/MenuWorks authority, pricing-source rules, area price order, exact MRN text handling, SSMT deletion authority, downstream eligibility, Supabase schema, production data, or release authority.
Seventh follow-up on 2026-08-31: Tyler clarified the export and modifier workflow. Optional unknown Centric export fields should be blank instead of `[placeholder]`, while Centric ID columns should remain blank. New SSMT item rows should start with no attached modifier groups; adding a modifier group creates one modifier item line. Item rows should show the number of attached modifier groups so IT can see zero without opening the modal. Modifier groups should support lock/unlock, movement, and copy-to-clipboard/paste onto another item rather than copying onto the same item. Modifier item text should normalize to lower case and use lighter text weight for readability, while modifier group names remain user-controlled. SSMT menu deletion should require retyping the menu name exactly. The menu selector should surface edit-flag/status information, support hibernating menus, include a fifth `Menu Library` bucket for app-library reconciliation work, and allow menu records to move between buckets. Pricing rows should support editable regional prices, a modifier-only flag, high-to-low selector ordering, modifier-only prioritization in modifier price selectors, and `Price Category Modifier Free` at `0.00` for all areas. This approval remains scoped to app-side SSMT workflow records and does not itself authorize destructive Menu Library/App deletion, Supabase schema changes, production data rewrites, or the downstream source-authority bridge where Menu Library and Neighborhood Rotations consume SSMT submenus as their operating menu source.
Eighth follow-up on 2026-08-31: Tyler approved Phase 2 for the SSMT source-authority bridge after Chief identified the source-authority risk. The approved safe bridge is that IT-complete, non-hidden `Core`, `Global`, and `Menu Library` SSMT menus may provide downstream Menu Library / Neighborhood Rotation selector rows through the existing Recipe Library API read path. SSMT submenu dividers define downstream menu names, and rows below a submenu divider belong to that submenu until the next submenu divider. For menu names owned by SSMT-derived rows, the app may replace matching MenuWorks rows at read time so stale imported rows do not surface beside completed SSMT programming. This approval does not authorize destructive deletion of Menu Library/App/Webtrition records, Supabase schema changes, manual production-data rewrites, removing Webtrition Report Menu Index as removal authority, changing pricing authority, or coercing exact MRN text. Requesting Admin: Tyler. Approving Admin: Tyler. Admin of Record: Tyler.
Ninth follow-up on 2026-09-03: Tyler approved adding explicit SSMT save controls because modifier edits were not reliably saving during fast work and there was no clear recovery action after a shared-save failure. The SSMT menu selector, selected-menu editor, and modifier modal should each expose a save action that immediately writes the current app-side SSMT workspace through the existing shared SSMT storage record and gives the user a retry point if the shared save fails. Tyler also approved a four-slot visible modifier clipboard so common modifier groups can be saved into named slots and pasted onto other items later, an item-flag system that stores multiple flags per menu with reason, note, item name, and timestamp, and a `Report flags` email draft addressed to Alex and Tyler containing all saved flags for that menu. Row contrast between menu items and modifier lines should be increased for scanning. The existing Centric export should be re-verified against the approved report shape but not redesigned in this pass. This approval does not authorize Supabase schema changes, manual production-data writes, destructive Menu Library/App deletion, Webtrition Report Menu Index removal-authority changes, pricing authority changes, or MRN coercion. Requesting Admin: Tyler. Approving Admin: Tyler. Admin of Record: Tyler.
Rejected alternatives: treating app-side SSMT deletion as Webtrition/Menu Library deletion authority without a separate source-authority implementation; free-typing modifier prices outside the Pricing Structure; changing the area price order; changing Supabase schema or production data in this UI pass; regrouping Platform Home into broader workflow categories.
Implementation status note: application commit `89ecbc1d03b00e9d270d9d4bf448e9845e29113b` is LIVE as version `2026.08.28.001-ssmt-home-sections-modifiers` through Tyler-authorized Chief emergency release execution on 2026-08-28 after the normal worker agents were blocked by expired OAuth. Vercel production deployment `dpl_GtDEJHDd5D7BmjzzQjbQmXcYgVhf` completed successfully, the live public bundle contains the approved version stamp, and production Playwright coverage for `tests/browser/landing-tool-sections.spec.js` plus `tests/browser/ssmt-tool.spec.js` passed 6/6 against `https://project-d8v25.vercel.app`. No Supabase/schema/production-data change occurred.
Follow-up implementation status note: application commit `4e27a3e7782ec08f6d8b23f0baf5a0afea51b380` is LIVE as version `2026.08.28.002-ssmt-modifier-dialog-width` through Vercel production deployment `dpl_5gE6UyxescoeekR5qGAFCXQzczeM`. Production Playwright passed `tests/browser/ssmt-tool.spec.js` 6/6 against `https://project-d8v25.vercel.app`. No Supabase/schema/production-data, modifier persistence authority, pricing-source authority, workbook parsing, Menu Audit source framing, or Webtrition/Menu Library deletion authority changed.
Second follow-up implementation status note: application commit `a2f4adb1db522c90e7a4398dffe51e788be56e42` is LIVE as version `2026.08.28.004-ssmt-maximized-builder-density` through Vercel production deployment `dpl_Z94rhLWbhnQ42iVZgQp5iN7eQ7CP`. Production Playwright passed `tests/browser/ssmt-tool.spec.js` 7/7 against `https://project-d8v25.vercel.app`, covering dense modifier item lines and a maximized desktop SSMT builder that uses the available width, shows at least 10 item rows, and avoids horizontal scrolling at a 3000px viewport. No Supabase/schema/production-data, modifier persistence authority, pricing-source authority, workbook parsing, Menu Audit source framing, area price order, exact MRN handling, or Webtrition/Menu Library deletion authority changed.
Third follow-up implementation status note: application commit `87ea9e8073f712c1460f83f379b060c06b2f8501` is LIVE as version `2026.08.30.002-ssmt-lock-copy-phase` through Vercel production deployment `dpl_Boy5EkSwa57DNV5Qqn7xBTgq3rGb`. Production Playwright passed `tests/browser/ssmt-tool.spec.js` 8/8 against `https://project-d8v25.vercel.app`, covering the readable maximum width lock, prominent phase panel, all-items-locked phase gate, row-level lock/unlock, read-only click-to-copy fields, copyable area prices, larger price text, alternating row color, and locked-row edit/reprice/delete freeze. No Supabase/schema/production-data, shared SSMT persistence authority, pricing-source authority, workbook parsing, Menu Audit source framing, area price order, exact MRN handling, modifier behavior, or Webtrition/Menu Library deletion authority changed.
Fourth follow-up implementation status note: version `2026.08.30.003-ssmt-shared-workspace` was published to production as part of the August 31, 2026 `2026.08.31.001-ssmt-centric-export` release. The candidate loads a shared `ssmt|workspace|current` workspace record from `/api/storage/records?tool=SSMT`, saves normal SSMT workspace edits back to the secure storage API, keeps guarded browser localStorage as fallback/cache, and scopes the API read to only that SSMT record while using the existing compatible app_records storage bucket. Local TDD proof passed `node scripts/test-storage-backbone.mjs` and `node scripts/run-playwright.mjs tests/browser/ssmt-tool.spec.js` 9/9, including shared locked-row load and unlock-save proof. No production data rewrite was performed.
Fifth follow-up implementation status note: application commit `7a248dbef7804b2078af3375f89df495467f0bb1` is LIVE as version `2026.08.31.001-ssmt-centric-export` through Vercel production deployment `dpl_4a6TZZzKoFqiC6c822a7Hq6bybJ9`. The live HTML references `/assets/index-DY-te2V0.js`, that live index contains the version stamp, and the live SSMT chunk `/assets/SsmtTool-DewrBY4k.js` contains the export and shared-workspace markers. The selected-menu editor now has an `Export SSMT` action that downloads a Centric-shaped eight-tab workbook from the selected menu and modifier records, and the seed parser recovers 36 Global menus from screenshot-aligned workbook sheets while the selector alphabetizes menus within each group. Production Playwright passed `node scripts/run-playwright.mjs tests/browser/ssmt-tool.spec.js` 10/10 against `https://project-d8v25.vercel.app`. No Supabase schema change or production data rewrite was performed.
Sixth follow-up implementation status note: release head commit `6dd555cbc814bbdd531978f459dbb97fc7e78756` is LIVE as version `2026.08.31.004-ssmt-legacy-bucket-correction`. The live HTML references `/assets/index-DG3Hcib6.js`, that asset contains the `.004` version stamp, and production Playwright passed `node scripts/run-playwright.mjs tests/browser/ssmt-tool.spec.js` 10/10 against `https://project-d8v25.vercel.app`. This release applies Tyler's export/modifier/pricing/menu-selector follow-up and adds a one-time correction for older shared SSMT workspace records: missing seed menus are appended, legacy seed menu bucket assignments are corrected, and then a `seedMenuTypeCorrectionsApplied` flag preserves later manual drag/drop bucket moves. No Supabase schema change, manual production-data rewrite, destructive Menu Library/App deletion, or downstream SSMT source-authority bridge was performed.
Seventh follow-up implementation status note: application commit `0d383bc80abd78939c96fdfd6af713a1c9110394` is LIVE as version `2026.08.31.005-ssmt-derived-menus`. The live HTML references `/assets/index-1LZOfrzn.js`, that asset contains the `.005` version stamp, production API returned `source: supabase-recipe-items+ssmt-derived` with `ssmtDerivedRows: 2`, production SSMT Playwright passed 10/10, and production Neighborhood Rotations SSMT-derived submenu coverage passed 1/1 against `https://project-d8v25.vercel.app`. The release adds SSMT submenu divider rows, derives IT-complete SSMT menu/submenu rows from the shared `ssmt|workspace|current` record through `/api/recipe-library`, replaces matching SSMT-owned MenuWorks menu rows at read time only, and keeps destructive reconciliation out of scope. No Supabase schema change, manual production-data rewrite, destructive Menu Library/App deletion, Webtrition Report Menu Index removal-authority change, pricing authority change, or MRN coercion occurred.
Eighth follow-up implementation status note: application commit `bc16d46a521f107564d38b84264f92137757aaf7` and release-record commit `7887de9cf8653fdbdee00c4ba9bcb7ca3fe56367` are LIVE as version `2026.09.03.001-ssmt-manual-save-flags`. GitHub's Vercel status for the release-record commit reported `Deployment has completed` at `https://vercel.com/tylerl-s-projects/project-d8v25/719mdkdzycKp5ihHKZWX5o6pc3pj`, and production asset checks found the live version stamp, manual-save markers, modifier clipboard markers, flag-report timestamp markers, and Centric export markers in the deployed app. The release adds explicit `Save SSMT workspace`, `Save menu`, and `Save modifiers` actions; stores four visible modifier clipboard slots in the existing SSMT workspace payload; saves item flags on the selected menu and builds a Tyler/Alex flag-report mail draft; and darkens menu/modifier row separation. Local SSMT Playwright passed 11/11 after first failing on the missing manual-save UI and old row contrast, production SSMT Playwright passed 11/11 against `https://project-d8v25.vercel.app`, and focused storage, Centric export, seed, derived-menu source, release workflow, changelog timestamp, platform tool, Menu Audit, diff, and build guards passed. No Supabase schema change or manual production-data write occurred.

### 2026-08-27 - SSMT grouped menu selector and draggable IT row ordering
Tyler approved refining the live `SSMT` menu selector and menu-builder table after the first flow restructure. Menu records should be organized by menu type in this order: `Core`, `Global`, `Promotions`, `Thompson Hospitality`. Groups should be color-coded, and menus should be alphabetized inside each group so IT/Culinary can find records faster.

The selected-menu builder should allow Culinary/IT to add item rows, delete item rows, and drag/drop item or divider rows into the desired programming order. Divider rows remain titled Centric category separators inside one menu. The item table should show a blank editable `Fixy` field to the left of `Label`, keep `Label` normalized to all caps, make `MRN` a blank editable text field, remove the visible `FOH` label, keep calories as `N/A` unless the menu is `Promotion`, and place blank editable `Category` and `Secondary category` after SEA price before area prices.

Phase/status remains a per-menu property. In this app-side pass, phase/status and row edits are saved through guarded browser persistence so they survive reload in the same browser. This is not yet shared Supabase persistence across the group; a backend SSMT persistence pass is still required before phase/status can be treated as organization-wide source of truth. This follow-up does not change pricing-source authority, workbook parsing, Supabase schema, production data, Menu Audit removal authority, or the rule that Core/Global become downstream-eligible only after `IT complete`. Requesting Admin: Tyler. Approving Admin: Tyler. Admin of Record: Tyler.
Rejected alternatives: leaving the selector as one ungrouped list; keeping row order fixed to workbook/import order; keeping `FOH / Fixy` as the visible label; displaying non-Promotion calories as a workflow field; making phase/status a screen-only value that disappears on reload; treating browser persistence as shared group truth.
Implementation status note: application commit `9025b09affbe38792706b882158f7ac6c95c07aa` is LIVE as version `2026.08.27.002-ssmt-menu-row-ordering` through Vercel production deployment `dpl_Cfi71ZvBUjDvoy7vqwaL6EfJf5Lk`. The live public bundle `/assets/index-Bu84N9fV.js` contains the version stamp, and production Playwright passed `tests/browser/ssmt-tool.spec.js` 3/3 against `https://project-d8v25.vercel.app`. No Supabase/schema/production-data change occurred.

### 2026-08-27 - SSMT workflow split and full-width menu editor
Tyler approved restructuring the live `SSMT` flow after the initial release because the single post-passcode screen was too crowded for large menus. After passcode `0411`, SSMT should split into two primary paths: `Pricing Structure` and `Menu Selector / New Menu`. Pricing Structure should let Culinary view the controlled pricing table and add pricing rows while keeping SEA price + category as the selector authority. Menu Selector should open existing menus or create a new menu only after selecting the menu type.

The selected-menu editor should use a fuller-width workspace for large menus. It must show a slim selected-menu navigation above the record with back to menu selection, pricing table, and delete menu actions. Delete menu requires retyping the menu name exactly before the destructive app-side record removal. Menu rows must include a typed Category field, a FOH / Fixy field for IT programming, and all area prices in Tyler's required IT programming order: `AUS, BNA, BOS, BWI, DEN, IAD, JFK, LAX, SAN, SNA, SEA, SJC, WAS, YVR, YYZ, MCO`. Menus may include titled divider rows so one Centric menu can be separated into internal programming categories such as grill and soups.

Active start/end dates are required only for `Promotion` and `Thompson Hospitality` menu records; Core and Global menus do not need those active date fields in the normal editor. This follow-up does not change SSMT workbook parsing, pricing-source authority, Menu Audit removal authority, Supabase/schema state, or production data. Requesting Admin: Tyler. Approving Admin: Tyler. Admin of Record: Tyler.
Rejected alternatives: keeping pricing, selection, and editing on one dense screen; requiring active start/end dates on every menu type; keeping category as display-only text; deleting menus without exact-name confirmation; hiding non-SEA area prices from the menu editor.
Implementation status note: application commit `68db163d6fedcd51e35b45e668b2e0ed86945cc4` is LIVE as version `2026.08.27.001-ssmt-flow-restructure` through Vercel production deployment `dpl_3vhmQemNEVmcHhBjxuL86JxbhvLX`. Production Playwright passed `tests/browser/ssmt-tool.spec.js` 2/2 against `https://project-d8v25.vercel.app`. No Supabase/schema/production-data change occurred.

### 2026-08-26 - SSMT app replaces the workbook, adds workflow/calendar, and resets Menu Audit sources
Tyler approved replacing the current SSMT Excel workflow with an in-app `SSMT` tool on the Culinary Tools Platform, gated by the shared hard-wired passcode `0411` as a convenience access gate rather than identity proof. The uploaded `SEA Standard Menu Template (1).xlsx` workbook is the current truth for building the first SSMT tool structure, pricing book, modifier relationships, workflow records, and candidate SSMT alignment flags. It is not deletion authority for Menu Library/Webtrition operational records on the first pass; item removal remains governed by the existing Webtrition Report Menu Index process until explicitly changed. First workbook import scope is limited to menus reachable from `Navigation UI` buttons, any hidden tab reached by one of those buttons, and Alex's recent Promotion tabs. Old hidden tabs that are not button-reachable stay out.

The approved SSMT product model is a workflow tool, not a spreadsheet clone. Menu records must carry menu type (`Core`, `Global`, `Thompson Hospitality`, `Promotion`), workflow phase/status, active dates, completed date, timestamps, and edit/resubmission state. Labels normalize to ALL CAPS, descriptions normalize to all lower case, and calories apply only to Promotional menus. Modifier detail opens in a pop-up/modal from the item row, clicking the background closes it, and copied modifier groups must become independent copies rather than linked shared records. Flagging stays inside SSMT as the system of record with saved flag rows, dropdown reasons led by `Description correction`, short notes, and a `Report` action that emails Tyler and Alex; email notification is secondary to the saved in-app flag state.

Pricing remains controlled by the pricing structure rather than free typing. Users choose SEA `price + category`, the tool fills the related area prices, and the app always shows areas in this exact order: `AUS, BNA, BOS, BWI, DEN, IAD, JFK, LAX, SAN, SNA, SEA, SJC, WAS, YVR, YYZ, MCO`. Tiers remain associated with those areas for now and are not collapsed to a repetitive Tier 1 / Tier 2 model until later E15 guidance changes the structure. The pricing book may accept added rows, but SEA price/category remains the programming selector authority.

Downstream eligibility also changes. `Core` and `Global` menus become eligible for Menu Library, Menu Audit operational readiness, and Rotations only after `IT complete`. `Thompson Hospitality` and `Promotion` menus are still saved as historical SSMT records, but they are excluded from those operational selectors. Workflow phases are Culinary draft/submit, Experience review/submit to IT, and IT complete/Centric ready, with explicit return/edit signaling from Experience or IT back to Culinary. Active dates also feed an SSMT calendar view.

Menu Audit is reset to the new source model. After replacement, the old SSMT Excel workbook is no longer an ongoing audit source. Audit readiness compares SSMT app records against the existing Webtrition Report Menu Index metadata and Webtrition Shopping Lists for cross-utilization support, while recipes are called out as the remaining missing source layer. The future audit output should distinguish ready records, missing metadata, mismatches, not-IT-complete records, excluded menu types, and SSMT/Webtrition review flags. SSMT-only absences must become review flags, not destructive deletes, unless the Webtrition Report Menu Index confirms removal authority.

Source: Culinary Tools Project channel, thread rooted at event `d224d92a17a1f3df4fa5a18495f6f88d32758a15d9c8d1e42eca1be1d41428bc`, Tyler scope clarifications on 2026-08-26, Tyler's explicit green light to build the SSMT tool + calendar + Menu Audit redo, Tyler's follow-up narrowing deletion authority on 2026-08-26, and Chief's mission assignment/scope correction on 2026-08-26. Requesting Admin: Tyler. Approving Admin: Tyler. Admin of Record: Tyler.
Rejected alternatives: keeping the workbook as an ongoing operating/audit source after replacement; deleting Menu Library/Webtrition operational records based only on the SSMT workbook; treating `0411` as real user identity; allowing free-typed prices outside the pricing structure; collapsing area pricing to Tier 1 / Tier 2 now; making copied modifier groups stay linked across menus; making `Core`/`Global` downstream-eligible before `IT complete`; exposing `Thompson Hospitality` or `Promotion` menus in operational selectors; relying on Teams/email instead of saved in-app flag records; continuing Menu Audit as a three-way comparison against the retired workbook.
Implementation status note: application commit `79a0656b0e1f38fdc691f6731266e6860d11a125` is LIVE as version `2026.08.26.001-ssmt-tool-menu-audit-source` through Tyler-authorized Chief emergency release execution on 2026-08-27 after the normal Reviewer, Verifier, and Release agents were blocked by expired OAuth. Vercel production deployment `dpl_82oFq2v5Ga5AbTUn3qvXkwGZ3K9Z` completed successfully, the live public bundle contains the approved version stamp, live seed data contains 56 menu records, 592 modifier groups, 62 pricing rows, and 61 Navigation UI targets with zero unmatched raw workbook prices assigned, and production Playwright coverage for `tests/browser/ssmt-tool.spec.js` passed 2/2 against `https://project-d8v25.vercel.app`. No Supabase/schema/production-data change occurred.

### 2026-08-23 - Menu Cross Utilization Tool phase-1 snapshot
Tyler approved a new `Menu Cross Utilization Tool` on the Culinary Tools Platform home screen as a guidance-only planning aid for the exact `Amazon Region (FBE000)` snapshot `Aug 3, 2026`, `Lunch`. Phase 1 is read-only and repo-managed: the checked-in shopping-list PDFs are the ingredient truth for overlap calculations, while the attached workbook is reference-only for pillar presentation and validation checks. The tool shows the document-defined pillar strategy, a full menu list, and a pairwise overlap matrix generated from shopping-list ingredient sets. The sole overlap score is plain Jaccard similarity on approved ingredient match keys (shared / union). `Chickle` remains visible with an explicit no-ingredient-data state and is excluded from the matrix so missing data is not misread as confirmed zero overlap. This tool is not an automatic rotation rule and must not claim prep overlap, recipe overlap, labor overlap, volume overlap, cost, savings, or waste reduction. Phase 1 does not include an upload workflow, Supabase writes, schema changes, production data writes, or any other runtime persistence path.
Source: Culinary Tools Project channel, thread rooted at event `40a0250551978a1a19ad9314b29a971fe7e0feabe3028225cf339cb399836577`, Tyler request and scope clarifications on 2026-08-23, Tyler's dependency approval, and Chief's scoped build brief. Requesting Admin: Tyler. Approving Admin: Tyler. Admin of Record: Tyler.
Rejected alternatives: treating the workbook as primary ingredient truth; presenting the tool as an automatic rotation authority; including `Chickle` in the matrix as a misleading all-zero row; adding upload UI or runtime file ingestion in phase 1; introducing Supabase/schema/production writes; making claims about prep, recipe, labor, volume, cost, savings, or waste beyond shopping-list ingredient overlap.
Implementation status note: application commit `aa16347f74dbf3b898e24672f8fb7468b12d9f62` is LIVE as version `2026.08.23.001-menu-cross-utilization-tool` through Tyler-authorized Chief emergency release execution on 2026-08-23 after the normal Reviewer, Verifier, fallback reviewer, and Release agents did not respond in-thread. PR #5 is closed/merged, the live public bundle contains the approved version stamp, and production Playwright coverage for `tests/browser/menu-cross-utilization.spec.js` passed 3/3 against `https://project-d8v25.vercel.app`. No Supabase/schema/production-data change occurred.
Display refinement note: Tyler approved the matrix display follow-up in-thread on 2026-08-23. Version `2026.08.23.002-menu-cross-utilization-matrix-display` keeps the same data authority and scoring decision, but the matrix view now displays pillar cross-utilization percentages, uses a red heatmap, enlarges cells, and prints each pair's overlap percent directly inside its square.

### 2026-08-16 - Reference plate-component terminology and automatic station pricing
Alex approved `Sub Recipe` as the canonical Culinary Platform label for every composed component formerly called `Plate Add` in the food-cost reference. House of Teriyaki and Anisa use the planner's existing Sub Recipe selections to satisfy those build requirements; Anisa's combined menu may use sides and Sub Recipes from its Lebanese and Persian reference stations while remaining isolated from other menus. Dedicated Fish Market LTO planners automatically price two distinct Fish Market sides plus one sauce without operator component fields. Core Grill automatically presents four named plate prices per sandwich using the two cheapest and two most expensive unique Grill Core reference sides. Automatically sourced Fish Market and Grill components are planner-only calculations, do not become chef selections, and are not persisted. Extensions remain separate.
Source: Alex's troubleshooting request and three clarifications in the current Codex task on 2026-08-16. Admin of Record: Alex Neuse.
Rejected alternatives: retaining the `Plate Add` label; requiring invisible component fields; treating Fish Market Lemon Wedge as its sauce; requiring Core Grill operators to choose sides before seeing economics; persisting inferred sides or sauces; mixing components across menus.
Implementation status note: application commit `65ea8568b9e67ebce73e10f2e717e2e6f1ee774c` is LIVE as version `2026.08.16.002-reference-plate-component-corrections` through successful Vercel production deployment `5933244767`. Both required production browser suites passed 43/43 with the approved terminology, automatic-component calculations, non-persistence, promotion, extension, and planner-only boundaries intact.

### 2026-08-16 - Reference plate-cost rollout across eligible Neighborhood Rotation planners
Alex approved expanding the reference-driven plate-cost presentation from the Nessie test to every current and future selectable Neighborhood Rotations planner whose selected or fixed menu exists in `docs/FOOD_COST_PLATE_COSTING_REFERENCE.md`. Reference eligibility is menu-based rather than café-, district-, or station-name-based. Menus missing from the reference retain legacy planner analytics. A selected reference menu with incomplete item identity, Item + Waste Cost, required build data, or primary Sell Price must show `Reference data unavailable` and no calculated range; this also covers resolved entrée/plate rows that are component-only or otherwise non-calculable, and suppresses every range when mixed with an otherwise complete plate. The primary entrée/plate Sell Price remains the denominator; the range spans every valid selected required-component combination. Extensions are always excluded from plates and show their own cost, sell price, and food-cost percentage. Promotion overrides negate the reference interface because promotions have no in-scope Culinary Platform pricing. The expansion is planner-only and must not replace recaps, Results, summaries, exports, persisted header economics, storage schemas, or Supabase/Smartsheet authority. Historical weeks retain legacy analytics.
Source: direct request and six clarified rollout decisions from Alex in the current Codex task on 2026-08-16. Admin of Record: Alex Neuse.
Rejected alternatives: applying the interface to stations whose menus are absent from the reference; substituting legacy MenuWorks cost when reference data is incomplete; pricing promotions; including extensions in plate cost; replacing Results/exports/persisted economics; retroactively changing historical selectable weeks.
Implementation status note: application commit `2fa7c18804bf6347d32f3cf119ef63335a19cea9` is LIVE as version `2026.08.16.001-reference-plate-cost-rollout` through successful Vercel production deployment `5932762278`. `pnpm run verify` passed before publication, and both required production browser suites passed 41/41 with the approved rollout boundaries, unavailable-data suppression, extension treatment, and planner-only isolation intact.

### 2026-08-14 - Nessie Global per-plate food-cost pilot
August 15 amendment approved by Alex: within the same exact pilot scope, the published food-cost Markdown reference now replaces the inferred MenuWorks composition rules. Each selected priced `Entree`/`Plate` primary's row-level `Plate Build` generates its required component selectors; when that field is blank, only a simple Concept Guide build that is neither `Unknown` nor `Varies by station` may fill it. `Item + Waste Cost` replaces true cost only in this pilot, the selected primary's `Sell Price` remains the denominator, and the range spans every valid combination among selected required components. Concepts without a priced primary build are unavailable instead of receiving an invented denominator; multi-station concepts require an explicit station choice. Extensions are excluded from plates and show separate individual economics. Identically named items remain isolated by menu/station/MRN/portion, and unclassified `AMZ+RA:` concepts must not be inferred. Version `2026.08.15.002-nessie-reference-plate-builds` is LIVE from application commit `d46fe818de042d68b74ea2b2e3f38816ea999e0b`; Vercel production completed successfully and both required live browser suites passed 39/39, including exact pilot calculations, persistence/recall, header completeness, extension separation, RA exclusion, and adjacent-week isolation.

Alex approved a planner-only per-entree plate food-cost pilot for North/Nessie Global in the exact week `Aug 17, 2026 - Aug 21, 2026`. Standard menu plates consist of one selected entree, one automatically identified selected starch/grain base, two selected non-base sides, and every selected sub recipe. Explicit source categories are authoritative for starch/grain bases (including pasta such as Smokehouse `Mac & Cheese`), with a rice/noodle item-name fallback and a salad-category veto. The exact carb-heavy exception menus `AMZ: Piccola Italia`, `AMZ: Lemongrass + Lime`, and `AMZ: Chiang Mai` instead use one entree, one selected side, and every selected sub recipe. Extensions are excluded. When multiple valid bases or sides are selected, the displayed range uses the least- and most-expensive valid plate combinations, and every percentage divides by the entree retail price only. The pilot appears only in the live Nessie Global planner for that week; submitted recaps, Results, summaries, exports, persistence, and every other cafe/week retain existing behavior.
Source: direct request and clarifications from Alex in the current Codex task on 2026-08-14. Admin of Record: Alex Neuse.
Rejected alternatives: adding a separate Base selector; continuing to divide by combined entree-and-side retail; including extensions; applying sub recipes only to selected entrees; immediately scaling the calculation to other stations, cafes, weeks, recaps, Results, summaries, exports, or persistence.
Implementation status note: the initial pilot application commit `0fa3c374cdb5b0629f2ddde456222576ed4788cc` was published and production verified as version `2026.08.14.001-nessie-global-plate-cost-pilot`. Alex's Smokehouse correction shipped in application commit `5f22b9811b2b4fa13da44abcd58a2ad3c389ded3`; Vercel reported production success, the live bundle contains version `2026.08.14.002-smokehouse-starch-base-fix`, and both required browser suites passed 39/39 against production. No schema, persistence, or source-authority change occurred. Release state is `LIVE`.

### 2026-08-05 - East District `AMZ: Balti` duplicate exception
Tyler approved a narrow Neighborhood Rotations duplicate-menu exception: within the East district only, `AMZ: Balti` may be selected by more than one cafe in the same week without blocking submission or counting as a leadership duplicate-menu problem. This exception applies only to the `AMZ: Balti` Global Menu label and only to East cafes; every other East duplicate rule stays unchanged, and South's existing `MENU_CONFLICT_GROUPS` behavior plus the `Re:Invent` exception remain unchanged.
Source: Culinary Tools Project channel, thread rooted at event `e97feca4d30b05f595b4050d2c356452a921060dd2c680e5a74f246d0e0c1f4f`, Tyler request on 2026-08-05, with Chief's scoped mission assignment and subsequent Reviewer/Verifier clearance on the amended candidate. Admin of Record: Tyler.
Rejected alternatives: keeping the blanket East cross-cafe duplicate blocker for `AMZ: Balti`; broadening the exception to all East duplicate menus; applying the exception outside East; using a broader cafe-level duplicate exemption instead of a menu-scoped East-only rule.
Implementation status note: app behavior commit `1200027e9b0528d74738584337ea06ee71ed4f6b` was published to GitHub `main` through release/docs head `b30ba360569e0870ddf779e8bd0edf5c7bbd801b` and independently confirmed live on Vercel asset `/assets/index-CA3kPasv.js` with version `2026.08.05.001-east-balti-duplicate-exception`; live production verification passed `neighborhood-rotations` 15/15 and `reinvent-submit-recall` 22/22. No schema, source-authority, or production-data change occurred. Release state is `LIVE`.

### 2026-08-01 — Single-final Release closeout and pre-release release-speed safeguards
Tyler approved tightening the deploy-intended release process so the admin-facing thread gets one Release closeout instead of a string of post-deploy chatter, while still preserving independent review, verification, and architecture safety. For deploy-intended releases, Release now owns the single final admin-facing result; Chief does not post a duplicate closing summary afterward, and Reviewer/Verifier/Scribe detailed verdicts stay in working threads unless they surface a blocker. Scribe's detailed record check can stay there too, but one short affirmative Scribe confirmation must still be surfaced in or quoted by Release from the admin-facing closeout before `LIVE` is declared. Tyler also approved four speed safeguards around that closeout: Scribe reconciles `AI_HANDOFF.md` against the release candidate before Release completes production verification; visible/user-facing changes must ship their first implementation commit with a direct automated acceptance test for the exact requested behavior; Builder/Reviewer/Verifier reuse one prepared worktree with dependencies and Playwright browsers already warm; and every Release final report carries a timing block plus avoidable-delay notes.
Source: Culinary Tools Project channel, thread rooted at event `49319ef19d9ae9148dc3f67ca5cbaab360c32f416b15480d9d88736c7422baa2`, Tyler messages on August 1, 2026 asking to reduce post-deploy chatter and then authorizing implementation of all speed recommendations, followed by Chief's scoped process-change assignment on August 2, 2026. Admin of Record: Tyler.
Rejected alternatives: allowing Chief to post a second final deploy summary after Release; discovering stale `AI_HANDOFF.md` only after production verification is already complete; approving visible behavior changes on config/text checks or adjacent suites that never exercise the requested behavior; creating new worktrees mid-mission and treating ownership/browser-install failures as normal release delay; omitting timing and avoidable-delay capture from release closeout.
Implementation status note: process documentation updated in `RELEASE_RUNBOOK.md` and `CHANGELOG.md`; no application code or visible app version change.

### 2026-08-01 — Streamlined deploy-intended default and micro-fix lane
Tyler approved changing governance so ordinary app changes requested by a Release-Authorized Admin are deploy-intended by default. Chief should no longer stop after implementation/review merely to ask for a separate `deploy` message; after the scoped change passes review and verification, Chief continues into Release Gate, production deployment, live verification, and final exact-state reporting unless the request says `do not deploy`, `local only`, or `investigate only`, or a real safety blocker appears. Tyler also approved a micro-fix lane for tiny, well-understood presentation, formatter, stale-test, and documentation/process fixes: one owner, one Reviewer, one Verifier, narrow proof, then Release Gate when deploy-intended.
Source: Culinary Tools Project channel, thread rooted at event `73da3848ad5cc3bc0bcad6c5fdb2b11425a1edd6e9dc458b50019e5249448690`, Tyler messages on 2026-08-01 asking Chief to always understand normal changes as deployment-intended and then to change the governance documents to encourage streamlined deployment. Admin of Record: Tyler.
Rejected alternatives: continuing to require a second `deploy` prompt after every completed change; treating every visible one-line cleanup as a full large-change mission; allowing deploy-intent to bypass review, verification, data/schema/destructive safeguards, or production live verification.
Implementation status note: governance/process documentation updated in `GOVERNANCE.md`, `MISSION_TEMPLATE.md`, `RELEASE_RUNBOOK.md`, `AGENTS.md`, and `CHANGELOG.md`; no application code or visible app version change.

### 2026-08-01 — Dawson Moby Pop-Up projects into Moby Global
Alex approved a same-week presentation override for the North District's Moby cafe: once Dawson submits valid Moby Pop-Up data, that submitted Dawson menu and its entree/side/sub-recipe/extension selections replace Moby's Global content across cafe tiles, Results, exports, and summaries. Moby retains its own Global selector for weeks without a qualifying Dawson submission and for future editing. Moby's own Draft/Submitted state and completion progress remain independent, and its Pizza, Salad, Deli, and Fresh Five stations remain native. A Dawson Moby promotion follows the same projection rule and affects only Moby Global. Duplicate-menu reporting is explicitly excluded and continues to evaluate Moby's native Global menu.
Source: direct request and clarifications from Alex in the current Codex task on 2026-08-01. Admin of Record: Alex Neuse.
Rejected alternatives: changing Moby's status/progress when Dawson submits; replacing Moby's non-Global stations; persisting a copy of Dawson data into Moby's saved rotation; counting Dawson's projected Moby menu in duplicate-menu reporting.
Implementation status note: published to GitHub `main` at application commit `16683504501f584d1a565c915b35cbcf904ccb46` and verified on Vercel production deployment `dpl_CE363147117z2vqmdNmYrRnKGtrR`; both required production browser suites passed 32/32 and independent Scribe LIVE sign-off passed. Release state: LIVE.

### 2026-08-01 — Dawson Moby Pop-Up station
Alex approved a Dawson-only Neighborhood Rotations station named `Moby Pop-Up`, required for submission beginning with the week of Aug 31, 2026 and explicitly operating Tuesday through Thursday. The station mirrors Dawson Global's menu-first planning format while allowing both Global menus and `AMZ: Carvery`, with capacity for 2 entrees, 3 sides, 2 sub recipes, and 1 extension. Alex also approved an isolated Moby promotion override with individually selectable Tuesday/Wednesday/Thursday service days, including one-day promos; enabling it hides and replaces the full normal Moby selection set for that saved week.
Source: direct request and clarifications from Alex in the current Codex task on 2026-08-01. Admin of Record: Alex Neuse.
Rejected alternatives: making Moby optional; showing it before the Aug 31 week; allowing Monday or Friday promo days; preserving normal Moby fields alongside an enabled promo; sharing Moby's override state or saved records with Dawson Global or Carvery.
Implementation status note: version `2026.08.01.001-dawson-moby-pop-up` was published to GitHub `main` at application commit `0b4ac627eb51bb68f8e92dd0fc4edb5435efef74` and verified on Vercel production deployment `dpl_2CRuWUbLK7AKR9rVNQAhYg54Cwgi`; both required production browser suites passed 31/31.

### 2026-07-31 — Bingo Cafe Wed-Tues Global cadence and second Grill Fresh $5 slot
Tyler approved a Bingo-only Neighborhood Rotations change: Bingo's Global Rotation should follow the same single Wednesday-Tuesday cadence pattern used by Doppler, and Bingo's Grill Fresh $5 station should expose two independently selectable `grillFreshFive` slots instead of one. This is explicitly not a split-global conversion: Bingo remains a one-menu Global cycle with Wednesday start / Monday-Tuesday carryover behavior, and the second option is a second `Grill Fresh $5` slot, not a second generic Grill `Location Spotlight`.
Source: Culinary Tools Project channel, thread rooted at event `9dc3ad58cd58ae10ddbf4bc027f78b5d7c3e9e732ddc20c995582d4f8b8d6aed`, request from Tyler on 2026-07-31, with Chief assigning scoped implementation and then directing Scribe to record the accepted behavior after Builder/Steward/Reviewer/Verifier clearance. Admin of Record: Tyler.
Rejected alternatives: converting Bingo to the Re:Invent/Blueshift split-global model; interpreting the request as a second generic Grill `Location Spotlight` instead of a second `grillFreshFive` slot.
Implementation status note: implemented and independently verified locally at commit `ef2aa015338ad4eef716aa80e79afe98323aaf82`; not merged, not deployed, not live as of this entry.

### 2026-07-11 — Hide Ladle Compliance from navigation
Hidden from the platform home screen and mobile tool navigation as of version `2026.07.11.003-hide-ladle-compliance`. The underlying code remains in the repo for a future rebuild; users should not be routed into it from normal navigation while it is unfinished/non-working.
Source: `AI_HANDOFF.md`, Ladle Compliance section.

### 2026-07-18 — Lean Tool email report recipients exclude Bil Smith
As of version `2026.07.18.002-lean-report-recipient-cleanup`, Lean Tool email report recipients intentionally exclude Bil Smith. Guarded by `scripts/verify-lean-results-view.mjs`.
Source: `AI_HANDOFF.md`, Lean Tool section.

### Webtrition is an external tool, not an internal React tool
Webtrition's platform-home card opens `https://www.webtrition.com/ui/#/` in a new tab using `public/webtrition-logo.png`, rather than being routed as an internal tool. Recipe deep-links may collapse to the base Webtrition app or show access errors depending on the user's Webtrition session/region/role — this is treated as expected external-tool behavior, not a bug to fix inside this repo. Menu Library instead opens plain MRN search links in a new tab with a Copy MRN fallback.
Source: `AI_HANDOFF.md`, Landing Page section. No version stamp recorded in source; not fabricated here.

### District Chef / SSMT Owner default assignment
The Menu Projects District Chef / SSMT Owner fields are hard-wired to Tyler Leiss and Alex Neuse.
Source: `AI_HANDOFF.md`, Menu Projects section. No version stamp recorded in source; not fabricated here.
Note: this is a product-data default, distinct from agent-governance authority — see [GOVERNANCE.md](GOVERNANCE.md) for the rule that no *agent* permanent instruction may hard-code an individual as sole authority. This existing product-data decision is carried forward unchanged and was not in scope for the 2026-07-25 governance reorg.

### Rejected alternative — append-only Recipe Library Supabase backfill
The Recipe Library Supabase backfill upserts current Master rows and marks visible Supabase rows absent from the Master as `visible_in_library: false`. An append-only backfill approach was tried previously and explicitly rejected: "do not reintroduce append-only backfill behavior."
Source: `AI_HANDOFF.md`, Menu Library section.

### 2026-07-25 — Agent organization reorg and governance documentation
Tyler reorganized the Buzz agent roster into 9 permanent workers + Chief (orchestrator) across 6 teams (Council, Triage, Build, Data Guard, Product Change, Release Gate), replacing the earlier ad hoc Fizz/Bumble/Honey lineup for governance/data/review/release work. Chief ran a governance audit confirming roster, teams, and permission boundaries; Tyler then approved building out this documentation hierarchy (this file and its siblings) to make that structure durable.
Source: Culinary Tools Project channel, thread rooted at event `31323e77e671b14520221fa55ff7a2a781b2f05b962b1f81adcf2039bbaf86de`, 2026-07-25. Admin of Record: Tyler.
Open item: no decision yet on formally decommissioning Fizz, Bumble, and Honey — see [GOVERNANCE.md](GOVERNANCE.md#legacy-agents--fizz-bumble-honey).

### 2026-07-26 — Fast Lane process for Level 1-2 missions
Tyler flagged that every request, regardless of size, was getting the full mission-brief-plus-full-team treatment, slowing down simple changes. Chief proposed two options: (A) permanently cut the permanent roster down to Chief/Scribe/Reviewer/Release/Builder, or (B) keep the full roster and add a reduced-team "fast lane" for Level 1-2 work only, escalating back to full process if scope drifts. Tyler approved Option B. The streamlined lane rule now lives in [GOVERNANCE.md](GOVERNANCE.md#micro-fix-lane) after the 2026-08-01 governance update; the Level 1-2 / Level 3-5 band split is also reflected in [MISSION_TEMPLATE.md](MISSION_TEMPLATE.md) § Notes.
Source: Culinary Tools Project channel, thread rooted at event `5b63927a6e855fa8f2b7021e33eb021a6ce1f50fd4794436a8e481f4a6b893b5`, 2026-07-26. Requesting Admin / Admin of Record: Tyler (approval event `7edea9a1...`, 2026-07-26T23:28:52Z).
Rejected alternative: permanently cutting the roster to Chief/Scribe/Reviewer/Release/Builder (Option A). Rejected because it would permanently remove independent verification (no Verifier) and data/product oversight (no Steward/Operator) for all future work, not just simple cases, rather than only for the class of request that's actually well-understood and low-risk.
